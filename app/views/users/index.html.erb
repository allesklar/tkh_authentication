<h1>Listing Users</h1>

<table class='table table-striped'>
	<thead>
  <tr>	
			<th>Name</th>
	    <th>Email</th>
			<th>Admin?</th>
	  </tr>
	</thead>
	
	<tbody>
		<% @users.each do |user| %>
		  <tr>
		    <td><%= user.name %></td>
				<td><%= user.email %></td>
				<td><%= user.admin? ? '✓' : 'X' %></td>
		  </tr>
		<% end %>
	</tbody>
</table>

<%= render 'shared/admin_sidebar' %>